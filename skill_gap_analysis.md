# travel-plan skill 落差分析與修正計畫

> **2026-09-11 更正**：使用者指出下方原始分析把 G1（同伴約束缺席）當成「最根本、其餘四項是其下游後果」不成立。
> 重新對照 `trips/2026-10-seoul/` 實跑產出後確認：**核心問題是覆蓋率／內容深度，不是約束建模。**
> 見文末〈6. 更正：覆蓋率才是根本問題〉，已據此在 `09_materials.md`、`10_itinerary.md` 補上硬規則（§7.3 第 8 條、規則 8 補充）。

- 日期：2026-09-11
- 對照組：`/home/lawrencechh/j/travel/src/posts/` 使用者親手撰寫的三份文件
  - `2026-09-09-首爾秋日漫遊手帳.md`（39.9 KB，17 張景點卡、30 筆美食）
  - `2026-07-13-首爾行前準備與緊急連絡資訊.md`（23.9 KB）
  - `2026-07-20-韓國自由行支付教學.md`（15.1 KB）
- 受測組：`trips/2026-10-seoul/`（skill 於 2026-09-09 實跑產出）
- 使用者評價：**「超級不實用」**

---

## 0. 結論先講

**不是 agent 不會找網站。** 本次已用實驗證明：同樣的網路環境下，agy（Gemini 3.8 Flash）
把 SES、K-ETA、AREX 票價、日出日落全部找到官方第一手來源，主 agent 逐條複驗全部屬實。
檢索能力的缺口是**規則沒寫好**，已於 commit `a276d57` 修掉（雙邊官方來源、403 改用 curl、
404 不等於整站不可達）。

真正的問題是**這個 skill 從一開始就在規劃另一趟旅行**——一趟給「一般觀光客」的旅行。
而使用者要規劃的，是一趟**四個人、其中有人走不快、有人不能吃辣不能吃內臟不能吃生食**的旅行。

使用者文件裡每一個欄位都在回答同一個問題：**「我們這一家人，這一站走得動嗎、坐得下嗎、吃得下嗎？」**
skill 的 schema 裡沒有任何欄位能承載這個問題的答案。

歸因：**檢索能力約 15%，產品設計約 85%。**

---

## 1. 五個結構性落差

### G1｜缺少「同伴約束模型」——整份文件的組織原則，skill 完全沒有抓到

使用者文件的每一張景點卡都有 `level:` 欄位，把地形難度分成四級：

| level | 出現次數 | 意義 |
|---|---|---|
| `flat` | 11 | 全線平坦，多設長椅 |
| `slope` | 4 | 坡道較多，需留意步調 |
| `steps` | 1 | 階梯／細沙碎石，鞋子要求高 |
| `diet` | 1 | 體力調節段（首日緩衝） |

30 筆美食裡，**22 筆標了「不辣」**（73%）、13 筆「軟嫩好入口」、10 筆「無內臟」、5 筆「無生食」。
這不是形容詞，是**硬篩選條件**——不符合的店家根本不會進入候選。

skill 這邊：`trip_context.json` 的 `party.mobility_notes` 是 `null`，
`style.pace` 是 `null`，`style.interests` 是 `[]`。
`intake.md` 沒有任何一步強制追問同伴的體力與飲食限制，於是整個規劃在**約束缺席**的狀態下進行，
產出的是一份給任何人的行程。

> **這是最根本的一條。** 其餘四項落差，有三項是這一項的下游後果。

### G2｜schema 記不下決策所需的資訊

| | 使用者文件 | skill schema |
|---|---|---|
| 地形 | `level` 四級 ＋ `tag` 文字（「細沙路面・建議著慢跑鞋」「人行道狹窄・部分入口多階梯門檻」） | 無 |
| 歇腳處 | `sub: 鄰近休憩處` **15 處具名**，且寫明為何可用（直達電梯、大沙發、冷暖氣、免費） | `rainy_day_ok: true/false` 布林值 |
| 飲食限制 | `不辣`／`無內臟`／`無生食`／`軟嫩好入口`／`需排隊!` 可組合標籤 | 無 |
| 價格 | `11,000 ~ 45,000 韓元` 實際區間 | `price_band: "₩₩"` |
| 地圖 | Naver ＋ Kakao 連結各 30 條（韓國 Google Maps 不可用） | 無 |
| 同伴友善度 | 上述欄位的組合，**可篩選、可據以取捨** | `friendliness.score` 1~5 整數 ＋ 自由文字 |

實跑產出的 7 個景點，`friendliness.score` 是 `[3,3,3,4,4,5,5]`。
**一個 3 分要怎麼用？** 不能篩、不能排序、不能告訴使用者「這站長輩走不動」。
它把一組本來可以機械判斷的硬條件，壓縮成一個無法行動的純量。

### G3｜輸出是敘述體，不是決策卡

使用者文件是**卡片式結構**：每一站固定回答〔地形等級〕〔為何值得去〕〔鄰近具名歇腳處＋為何可用〕
〔下一站怎麼移動、車程多久、叫什麼車〕。這是可以直接在路上打開手機照著走的格式。

skill 的 `10_itinerary.md` 是條列敘述：

> 上午：三清洞街 → 景福宮（草稿指名，含守衛交接儀式時段）
> Plan B：國立古宮博物館（`rainy_day_ok: true`，同區，室內）

同一個國立古宮博物館，使用者文件寫的是：

> 位於景福宮旁，館內展區均有直達電梯與大沙發休息區，且有冷暖氣，是絕佳的免費歇腳處。

**兩邊講的是同一個地方，但只有一邊能用。** 差別在於後者回答了「為什麼這裡能歇腳」。

### G4｜規模沒有規格，實跑只做了十分之一

| | 使用者文件 | skill 實跑 |
|---|---|---|
| 景點 | 17 張卡，橫跨約 8 個區域 | 7 個，2 個 area |
| 美食 | 30 筆 | 12 筆 |
| 地圖連結 | 60 條（Naver＋Kakao） | 0 |

`09_materials.md` 只規定「每個 area 觀光客 3＋當地人 3」，
但**沒有規定要建幾個 area**，schema 寫 `minItems: 1`。
於是實跑合規地只做了 2 個 area，然後在 `10_itinerary.md` 文末承認
鷺梁津、南怡島、首爾林等指名點「未依 09 規格建檔」——
那些景點的行程內容，其實是把使用者草稿原句抄進去的。

**使用者要的正是這些沒做的部分。** 他自己的文件裡有 **45 處「（內容待補）」**，
那 45 個洞就是他期待 skill 填的東西。skill 一個也沒填。

### G5｜給了選項清單，沒給決策

使用者文件的機場接駁段落，是四個方案配 ★ 評分 ＋ 一句 tagline 說明取捨，
並且**理由直接綁在這一家人的條件上**：

> 機場巴士 6701｜★★★★☆｜行李有專人提放至行李艙
> T2 B1 交通中心 18 號月台，**首班車 05:15（完美接上 05:00 降落之航班）**
> 於飯店所在路口下車，步行 100 公尺即達飯店後門

skill 的 `05_transport.md` 是一張沒有推薦的比較表，票價寫成
「約 ₩4,000-5,000」的模糊區間——而且**直達車票價 ₩11,000 是錯的**（官方 13,000），
掛的來源 `airportrailroad.com/intro` 那頁根本不含票價資料。

**使用者早在兩個月前就把 13,000 和 51 分鐘寫對了。**

支付教學那份文件更直接，標題就是決策規則：
「什麼情況才需要考慮明洞」「什麼情況不需要跑明洞」。
三份文件全部以「30 秒快速瀏覽 / 行前速覽」開頭——先給結論，再給依據。
skill 的產出沒有任何一份有這種收斂層。

---

## 2. 「是不是 agent 本質上就不會找網站？」

分項回答，每項都有本次實驗證據：

| 質疑 | 判定 | 證據 |
|---|---|---|
| agent 找不到官方來源 | **否** | agy 找到移民署 `egate.immigration.gov.tw`、駐韓代表部 `roc-taiwan.org`、AREX 官方票價頁、KASI 天文台，主 agent 逐條複驗全部屬實、對題、確為官方 |
| agent 找不到部落客實測 | **否** | 使用者引用的 `bobbytravel.tw`、`bigfang.tw` 都是公開可搜的站點；skill 實跑也引到了同類來源，只是選材軸不同 |
| 網路環境擋住了 agent | **部分，且影響小** | 4 個「不可達」網域裡只有 2 個是真的（DNS 失效、境外 IP 逾時）；`tripadvisor` 403 是反爬蟲、`feitravel.tw` 根本是誤報（實測 200） |
| WebFetch 的 403 造成誤判 | **是，但已修** | 日本外務省 `anzen.mofa.go.jp` 對 WebFetch 回 403、curl 帶 UA 回 200（72 KB 全文）。已寫進 `research_rules.md` §2.5 step 1.5 |
| 檢索策略單薄 | **是，已修** | 只往目的地國官網鑽、單一路徑 404 就放棄。已新增 §2.4 雙邊官方來源 ＋ §2.5 站內搜尋規則 |

**結論：檢索是可修的工程問題，且大部分已經修完。剩下的落差全部在產品設計層。**

一個關鍵反證：使用者文件裡最有價值的資訊——「國立古宮博物館有直達電梯與大沙發」、
「土俗村已全面改為一般桌椅席，無須盤腿」、「大林倉庫有大量軟質沙發座位」——
**這些都是查得到的**。skill 沒查，是因為它**不知道要查這個**。

---

## 3. 修正計畫

### P0（2026-09-11 二次修正）：飲食限制沿用既有的 `derived_constraints`，不另立專屬欄位

原 P0 把「地形/體力」與「飲食限制」綁成同一個 `party.constraints` 硬約束物件，使用者先拍板縮小範圍
（**地形/體力不用管**），接著又指出第一版的「簡化版」做法本身還是走偏了：另外新增
`party.diet_notes` + `food.dietary_tags` + 一條「不得臆測套用」的禁止規則，是**重複造輪子且更死板**——
`trip_context.derived_constraints` 本來就是為了裝這類「使用者講了什麼、下游就照著用什麼」的自然語言
限制而設計的欄位（範例：紅眼班機、末半天可用時數），飲食限制沒有理由不能比照辦理，不需要獨立的
enum 化欄位。而且「不得臆測」防的是一個**從沒發生過的失敗情境**——兩次實跑 agent 都沒有自己亂發明
飲食限制，屬於沒證據支持的防禦性規則。已撤銷該次改動：

| # | 動作 | 檔案 | 狀態 |
|---|---|---|---|
| P0-1 | ~~`trip_context.schema.json` 新增 `party.diet_notes`~~ | `templates/trip_context.schema.json` | ↩️ 已撤銷 |
| P0-2 | ~~`intake.md` 新增 `party.diet_notes` 抽取行~~ 改為：飲食限制等同其他使用者明講的限制，寫進 `derived_constraints` 即可 | `references/intake.md` | ✅ 已改 |
| P0-3 | ~~`food.schema.json` 新增 `dietary_tags`~~ | `templates/food.schema.json` | ↩️ 已撤銷 |
| P0-4 | `09_materials.md` 改為：飲食限制走 `derived_constraints`，沒講就不用主動問也不用臆測，維持 §9.2 熱門度/多樣性方法即可 | `references/sections/09_materials.md` | ✅ 已改 |
| ~~P0 舊版~~ | ~~`mobility`/`stairs_ok`/`max_walk_min`/`queue_tolerance` 等地形相關欄位~~ | — | ❌ 不做，使用者明確表示不用管 |

**教訓**：遇到「這裡好像缺一個機制」時，先檢查 skill 既有的通用機制（`derived_constraints`／
`named_musts` 這類自然語言陣列）夠不夠用，不要預設答案是「加一個新 schema 欄位 + 一條禁止規則」。
§9（教 agent 怎麼用 Naver 面板找候選）是這個 skill 該有的設計方向——**給方法、讓 agent 自由發揮**；
「不得臆測套用」這種防禦性 prohibition 是反例，只有在真的觀察到失敗情境時才該加，不是先加了再說。

### P1（部分作廢）：schema 換軸

原 P1 表格中「地形」相關項目（`spots.schema.json` 的 `terrain`/`rest_spot`，`food.schema.json` 的 `queue_risk`/`queue_workaround`、`seating`）**不執行**，理由同上。仍保留執行的部分：

| # | 動作 | 檔案 | 驗收標準 | 狀態 |
|---|---|---|---|---|
| P1-2 | `food.schema.json` 新增 `dietary_tags` | `templates/food.schema.json` | 已做，見 P0-3 | ✅ |
| P1-3 | 新增 `map_links`（Naver／Kakao／當地主流地圖，依目的地國決定）。**韓國、中國等 Google Maps 不可用的目的地為必填** | 兩份 schema | 每筆素材至少一條當地地圖連結 | ⏳ 未做 |
| P1-4 | `friendliness.score` **廢除**，改為由上述結構化欄位推導 | 兩份 schema ＋ `09_materials.md` | schema 內不再有無法行動的純量評分 | ⏳ 未做，且需重新評估——地形篩選拿掉後，`friendliness.score` 要廢除改推導什麼，定義要重想 |

### P2：規模、格式與收斂層

| # | 動作 | 檔案 | 驗收標準 |
|---|---|---|---|
| P2-1 | `09_materials.md` 訂定規模規格：**素材庫必須覆蓋 `named_musts` 的全部所在區域**，不得只做最低限度；受限於執行時間時要在 `progress.md` 列出未覆蓋清單與原因 | `references/sections/09_materials.md` | 實跑 2 個 area 的情況會被判為未完成 |
| P2-2 | `10_itinerary.md` 輸出改為**卡片式**：每站固定含〔地形等級〕〔停留時間〕〔具名歇腳處＋為何〕〔下一站移動方式＋車程＋車型〕 | `references/sections/10_itinerary.md`、`templates/section.template.md` | 產出可直接對照使用者文件的 `stop` 卡結構 |
| P2-3 | 每份成品開頭加「30 秒速覽」收斂層：先給結論與推薦，再給依據 | `templates/section.template.md` | 每份 `plan/*.md` 開頭都有 |
| P2-4 | 多方案比較一律附**推薦與理由**，理由必須引用 `party.constraints`；禁止只列選項不給結論 | `references/sections/01`、`05` | `05_transport.md` 出現「本次推薦 X，因為 Y」 |
| P2-5 | 數值型資訊（票價、時刻、費用）禁止寫模糊區間，查不到就標 `[待確認]`，不得用「約 4,000-5,000」蒙混 | `references/research_rules.md` | 成品中不再出現無來源的估算區間 |

### P3：既有錯誤修正

| # | 動作 |
|---|---|
| P3-1 | `trips/2026-10-seoul/plan/05_transport.md:10-11` AREX 票價改為官方值（直達 13,000／51 分鐘 T2、普通 4,750），來源改為 `/train/express/introduce` 與 `/train/normal/fare` |
| P3-2 | 全面複查實跑產出中「來源不支撐內容」的段落（`/intro` 那類） |

---

## 4. 明確不做的事

- **不做通用旅遊指南。** 這個 skill 的價值在於「為這一組特定同伴規劃」，不是產出維基百科。
- **不追求取代使用者的文件。** 目標是把那 45 處「（內容待補）」填到可用，
  排版與文案風格仍由使用者掌握。
- **不再增加檢索規則。** §2.4／§2.5 已足夠，再加只是把問題往檢索層推。

---

## 5. 建議執行順序

```
P0（同伴約束）─┬─► P1（schema 換軸）──► P2-1（規模）──► P2-2/3/4（格式與收斂）
               └─► P2-5（數值精度，可平行）
P3（既有錯誤）可隨時進行，與上述無依賴
```

---

## 6. 更正：覆蓋率才是根本問題，不是同伴約束

重新檢查 `trips/2026-10-seoul/materials/spots.json` 與 `plan/10_itinerary.md` 後，原第 0/1 節
「G1 是最根本、其餘四項是下游後果」這個因果鏈**站不住腳**，理由如下：

**兩個軸是獨立的，不是誰下游誰。** 同伴約束（走不走得動、吃不吃得下）回答的是「該不該推薦這個
選項」；覆蓋率／內容深度回答的是「這個選項有沒有被好好查過」。即使 `party.constraints` 全部補齊，
若素材庫只建了 2 個 area（本次實跑：`myeongdong`、`gyeongbok`），其餘 6 個草稿指名區域
（鷺梁津、汝矣島、南怡島、首爾林、德壽宮…）在 `10_itinerary.md` 裡依然只是把使用者自己草稿的
原句直接搬過去——**沒有 `why`、沒有 `sources`、沒有友善度評分、沒有具名歇腳處**。這幾天的行程，
skill 相對於使用者原始草稿的附加價值是 **零**。這正是「完全不知道在幹嘛」「交差了事」這個觀感的
直接來源，且與同伴走不走得動無關——就算內容全部標成「輕鬆好走」，一句從草稿複製貼上的行程，
讀起來仍然像沒做過功課。

**`TEST_REPORT.md` 缺陷 6 早就記錄了這件事**，但原本的 P0-P3 排序把它壓在 P2-1（優先度低於整份
約束建模），且措辭是「規模沒有規格」，容易被讀成次要的規格缺漏，而不是「產出的三分之二內容其實
沒做」這種嚴重度。

**已做的修正**（不等 P0，直接處理，因為這是獨立問題）：
- `references/sections/09_materials.md` §7.3 新增第 8 條硬約束：`areas` 必須覆蓋
  `trip_context.named_musts` 涉及的全部地理區域，`minItems: 1` 不再是可接受的完成標準；
  受限時必須在 `progress.md` 列出未覆蓋清單與原因。完成判準 checklist 同步新增對應項。
- `references/sections/10_itinerary.md` 規則 8 新增：素材庫沒建檔的指名點，行程本文**每次出現**都
  要標註「（未建檔，沿用草稿原文）」，不得只在文末待確認段落集中交代一次——避免讀者以為每一站
  都經過同等程度的查證。

**仍待處理**：P0（同伴約束）作為獨立問題依然成立且重要，但不再是「不做這個其餘都白工」的唯一
前提；覆蓋率規則已可獨立生效，兩者可平行推進，不必依序等待。

P0 未完成前不要動 P1 之後的任何一項——schema 要記什麼，取決於我們決定要問使用者什麼。

---

## 7. 美食發掘管道：兩階段來源（2026-09-11 追加）

G4/缺陷 6 只解決了「有沒有建檔」，沒解決「建檔的美食為什麼同質化、單一化」。使用者指出根本
原因：原本的檢索策略只會用「{目的地} 必去美食」這種泛用關鍵字撈到處都在轉貼的通用排行榜。

已在 `references/research_rules.md` 新增 §9，把發掘拆成兩階段，兩階段刻意用不同來源：
- **景點**：優先找「使用者所在國旅人」寫「目的地國」的遊記（貼近使用者背景的視角）。
- **美食**：改用「目的地國當地人常用的地圖/搜尋服務」找候選，跨不同分類取樣（韓國已用
  `search.naver.com` 整合搜尋實測驗證可行，含評分、評論數、`좌식`/`휠체어출입가능` 等篩選標籤）。

`09_materials.md` 檢索指引已加一句指向 §9。其他目的地國家的等效在地服務尚未逐一驗證，
§9 已註明「套用前必須先實測一次，不得假設每個國家都有 Naver 這種面板」。

---

## 8. 三套方案改為「1 套客製 + 2 套部落客實走行程」（2026-09-12）

使用者提出一個同時解決兩個問題的設計：`10_itinerary.md` 原本三套方案（經典必去/深度在地/輕鬆慢遊）
都是從同一份 09 素材庫拿去排列組合，這造成兩個成本：
1. 09 素材庫必須覆蓋所有 `named_musts` 區域才能撐起三套方案（見 §6 缺陷 6），建置成本隨區域數線性增加。
2. 三套方案的「客製」全部依賴使用者的私人資訊（`trip_context`），每一套都要走一次去識別化。

改為：**方案 A 維持現有客製邏輯**（09 素材庫 + `trip_context`，須涵蓋 `named_musts`）；
**方案 B、C 改成上網找同目的地、同天數、真人走過的完整逐日遊記，找到後只依使用者實際的
起訖時刻做可行性調整，其餘沿用原文骨架**，並附上原文出處。理由：
- 部落客發表的完整行程本身就是「已經實走驗證過可行」的路線，比 agent 自己拼裝更可靠，
  也不需要自己捏造——這點規則裡明寫「不得自行拼裝或想像」。
- 方案 B、C 不再需要涵蓋 `named_musts`，09 素材庫的建置壓力回到只需支撐方案 A 一套。
- 方案 B、C 只用到 `trip_context` 裡本來就允許保留、不需去識別化的欄位（起降時刻、`stay.checkin`），
  沒有額外的隱私處理需求。
- 為避免每次都抓同一篇文章，規則要求找到多篇合格候選後刻意輪替/隨機選 2 篇不同作者的。

已改寫 `references/sections/10_itinerary.md` 規則 5（重寫）、規則 6（Plan B 限定方案 A）、
規則 7（併入規則 5，作廢）、規則 8（限定方案 A），以及對應的必答問題清單、檢索指引、
完成判準 checklist、常見錯誤段落。

---

## 9. 產品定位修正：核心產出是 materials/，行程安排只是參考草案（2026-09-12）

使用者明確拍板：這個 skill 真正要解決的問題是「讓自由行旅客快速拿到目的地國家最新、正確、
好玩的景點/美食資訊」，重點在 `materials/`；`10_itinerary.md` 的三套方案只是示範用法的參考草案，
使用者可以直接採用、自己以半天為單位重組，或拿去跟其他 agent 討論深化——**不是這個 skill 要
死守到滴水不漏的最終產品**。`trip_context.json` 裡使用者草稿的 Day1~DayN 只是「大方向」，不是
決定性骨架。

派了一個「資深旅遊嚮導」人設的 sonnet agent 審查改版計畫（避免自己閉門造車、也避免過度設計），
他的意見：定位轉變本身合理（`09_materials.md` 開頭本來就寫「素材庫與行程分離」，這次只是把
產品定位講清楚）；但提醒**如果只改 description、加欄位，卻不同步砍掉 `10_itinerary.md` 過重的
規格（規則 5 原本要求 ≥3 篇候選＋逐篇驗證＋輪替選 2 篇不同作者），會出現「嘴上說是參考草案，
規格卻要求做到滴水不漏」的自我矛盾**——這個提醒被採納，見下方異動。

**已做的異動**：
1. `SKILL.md` description 與內文改寫，明講核心產出是 `materials/`，行程安排是參考草案。
2. `spots.schema.json` 新增三個欄位，皆為**選填、非窮舉**（審查意見特別提醒不要做成完整距離矩陣）：
   - `areas[].nearby_areas`（`area_id`／`travel_min`／`mode`）：大區域之間的距離關係，
     `travel_min` 必須回溯到 `05_transport.md` 已查證的資料，不得憑空估算——避免把「防止 AI
     幻覺距離」的欄位本身做成幻覺。
   - `spots[].nearby_spots`（`id`／`walk_min`）：同一 area 內景點彼此的距離，單向、不要求對稱。
   - `spots[].best_time_notes`：時間敏感型景點（日出日落、潮汐）的建議時段，順手記、不特地查。
3. `09_materials.md` §7.3 新增第 9 條：上述兩個參照欄位只做「存在性」檢查，不檢查完整性/對稱性；
   `validate_materials.py` 同步加上輕量檢查（`validate_spots` 內）。
4. `10_itinerary.md` 加「本檔是參考草案，不是最終定案」的定位聲明；規則 5 的方案 B/C 產生方式
   降規格：找 1-2 篇合適文章即可（原本要求至少 3 篇候選＋逐篇驗證＋輪替選 2 篇不同作者），
   找不到合適文章時老實標「延伸自方案 A 素材」，不再要求兩篇一定要不同作者。

**沒做**（審查意見與 PM 皆未要求）：不建 area 兩兩配對的完整距離矩陣、不要求 `nearby_spots`
雙向對稱、不為每個景點特地查 `best_time_notes`、不新增 `trip_context.json` 欄位。
